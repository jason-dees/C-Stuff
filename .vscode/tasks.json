{
    "tasks": [
        {
            "type": "shell",
            "label": "run build.sh",
            "command": "./build.sh",
            "args": [
                "${fileBasename}"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}